{
  "name": "nexo-dashboard-pro",
  "version": "16.0.0",
  "description": "NEXO Dashboard PRO — ERP interno da NEXO Digital",
  "scripts": {
    "build": "cd backend && npm install && cd .. && cd frontend && npm install --legacy-peer-deps && npm run build && rm -rf ../backend/public && cp -r dist ../backend/public",
    "start": "cd backend && NODE_PATH=../node_modules node server.js",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "brain:sync": "node scripts/brain-sync.js",
    "brain:sync:principal": "node scripts/brain-sync.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "dotenv": "^17.4.2",
    "node-telegram-bot-api": "^0.67.0"
  }
}
